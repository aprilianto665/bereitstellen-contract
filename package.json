{
  "name": "bereitstellen-contract",
  "version": "1.0.0",
  "description": "Bereitstellen smart contract deployment on Sepolia",
  "scripts": {
    "test": "hardhat test",
    "deploy": "hardhat run scripts/4_deploy.js --network sepolia",
    "interact": "hardhat run scripts/4_interact.js --network sepolia"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "hardhat": "^2.19.0"
  },
  "dependencies": {
    "dotenv": "^16.3.1",
    "ethers": "^6.9.0"
  }
}
